media: media

content:
  - name: pages
    label: Blog Posts
    type: collection
    path: docs
    fields:
      - name: title
        label: Title
        type: string

      - name: description
        label: Description
        type: string

      - name: date
        label: Date
        type: date

      - name: category
        label: Category
        type: string

      - name: tags
        label: Tags
        type: string

      - name: body
        label: Content
        type: rich-text